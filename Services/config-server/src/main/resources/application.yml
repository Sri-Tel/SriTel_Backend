spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

logging:
  level:
    org.springframework.cloud.config: DEBUG

server:
  port: 8888
